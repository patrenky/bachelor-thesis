    //    public void displayImage() {
//        try {
//            // nacitanie obrazku z assets do ims a prevedenie do drawable
//            InputStream ims = getAssets().open("stlouis/stlouis1.jpg");
//            Drawable drawable = Drawable.createFromStream(ims, null);
//
//            // ukazatel na obrazok vo view + vykreslenie drawable
//            ImageView imageView = (ImageView) findViewById(R.id.myImage);
//            imageView.setImageDrawable(drawable);
//
//            // prevod drawable na byte[]
////            ByteArrayOutputStream baos = new ByteArrayOutputStream();
////            Bitmap bitmap = ((BitmapDrawable) drawable).getBitmap();
////            bitmap.compress(Bitmap.CompressFormat.JPEG, 100, baos);
////            byte[] imageInByte = baos.toByteArray();
//        } catch (IOException e) {
//            e.printStackTrace();
//        }
//    }


※※※Bitmap to byte※※※

            width = bitmap.getWidth();
            height = bitmap.getHeight();

            int size = bitmap.getRowBytes() * bitmap.getHeight();
            ByteBuffer byteBuffer = ByteBuffer.allocate(size);
            bitmap.copyPixelsToBuffer(byteBuffer);
            byteArray = byteBuffer.array();
※※※byte to bitmap※※※

        Bitmap.Config configBmp = Bitmap.Config.valueOf(bitmap.getConfig().name());
        Bitmap bitmap_tmp = Bitmap.createBitmap(width, height, configBmp);
        ByteBuffer buffer = ByteBuffer.wrap(byteArray);
        bitmap_tmp.copyPixelsFromBuffer(buffer);


        int A = (color >> 24) & 0xff;
        int R = (color >> 16) & 0xff;
        int G = (color >>  8) & 0xff;
        int B = (color      ) & 0xff;


AndroidManifest large android memory
https://stackoverflow.com/questions/32244851/androidjava-lang-outofmemoryerror-failed-to-allocate-a-23970828-byte-allocatio
https://developer.android.com/studio/intro/studio-config.html

 * luminance: https://stackoverflow.com/questions/596216/formula-to-determine-brightness-of-rgb-color
 * histogram: http://hdr-photographer.com/2014/03/tutorial-understanding-histogram/